# 桌面宠物 · 如何发布更新（给作者看）

## 如何更换 EXE 图标（自定义功能）

桌宠的图标有 4 个落点，都已支持自定义：
- 资源管理器里 EXE 的图标
- 右键属性 → 详细信息（产品名、版本、作者）
- 运行后任务栏 / Alt+Tab 图标
- 关于对话框

**换图标流程：**
1. 把你的图片覆盖 `assets/source_icon.png`（任意 PNG/JPG，建议 ≥256×256，正方形最佳；非正方形会自动居中裁剪）
2. ⚠️ **先关闭正在运行的桌宠**（否则 EXE 被占用）
3. 运行 `python build.py`
4. 产出 `dist/糊宠.exe`，图标、属性、任务栏全部更新

> 想改属性面板里的"产品名 / 作者"，编辑 `build.py` 顶部的 `APP_TITLE` / `COMPANY` 常量即可。

---

## 一次性准备（只需做一次）

### 1. 创建 GitHub 仓库
1. 登录 GitHub，点右上角 **+ → New repository**
2. 仓库名填 `DesktopPet_YMJH`，选 **Public**（公开），勾选 **Add a README**
3. 点 **Create repository**

### 2. 上传 version.json
1. 在新仓库页面点 **Add file → Upload files**
2. 把本目录的 `version.json` 拖进去，提交

### 3. 改程序里的 URL
打开 `desktop_pet.py`，确认这两行的用户名是你的（已替换为 `HannahZ-Mulan`）：
```python
VERSION_CHECK_URL = "https://raw.githubusercontent.com/HannahZ-Mulan/DesktopPet_YMJH/main/version.json"
VERSION_CHECK_URL_CDN = "https://cdn.jsdelivr.net/gh/HannahZ-Mulan/DesktopPet_YMJH@main/version.json"
```
（如需更换账号，改这里的用户名即可）

---

## 每次发布更新（3 分钟流程）

### 第 1 步：改版本号
打开 `desktop_pet.py`，把：
```python
APP_VERSION = "1.0.0"
```
改成新版本号，例如 `"1.0.1"`。

### 第 2 步：重新打包
⚠️ **先关闭正在运行的桌宠**（否则 `dist/糊宠.exe` 被占用，打包会报"拒绝访问"）。

在项目目录运行：
```bash
python build.py
```
脚本会自动完成：① 用 `assets/source_icon.png` 生成多尺寸图标 → ② 生成版本资源 → ③ 调 PyInstaller 打包。产物为 `dist/糊宠.exe`。

> 如果只想换图标不想重打包，单独跑 `python build_icon.py` 也能生成 `assets/app.ico`。

### 第 3 步：上传 EXE 到 GitHub Release
1. 在仓库页面点 **Releases → Create a new release**
2. **Choose a tag** 填 `v1.0.1`（和版本号一致）→ 点 **Create new tag**
3. **Release title** 填 `v1.0.1`
4. 把 `dist/糊宠.exe` 拖到 **Attach binaries** 区域
5. 点 **Publish release**

### 第 4 步：更新 version.json
1. 打开本目录的 `version.json`，改成：
   ```json
   {
     "version": "1.0.1",
     "update_date": "2026-08-01",
     "download_url": "https://github.com/HannahZ-Mulan/DesktopPet_YMJH/releases/download/v1.0.1/糊宠.exe",
     "changelog": "修复了XXX，新增XXX皮肤"
   }
   ```
   ⚠️ **关键**：`download_url` 里的 tag 号 `v1.0.1` 要和第 3 步的 tag 一致
2. 回到 GitHub 仓库，点 `version.json` → 右上角铅笔图标编辑 → 粘贴新内容 → Commit

### 完成！
用户下次启动桌宠时，会自动检测到新版本并提示更新。

---

## 用户端会发生什么
- 启动后 3 秒静默检查（不打扰）
- 有新版 → 右键菜单「检查更新」会弹出更新提示
- 点「立即更新」→ 下载进度条 → 自动替换 → 重启
- 下载失败 → 提示用浏览器手动下（点「打开下载页」跳转 Release 页）

## 常见问题
- **用户下不动**：GitHub 国内偶尔不稳定，让他用浏览器打开 download_url 手动下，或开梯子
- **用户没收到更新提示**：确认 version.json 的 version 比 APP_VERSION 大，且已 commit
- **想强制所有用户更新**：在 changelog 里写明，或把 download_url 换成最新版
