# 桌面宠物 · 如何发布更新（给作者看）

## 如何更换 EXE 图标（自定义功能）

桌宠的图标有 4 个落点，都已支持自定义：
- 资源管理器里 EXE 的图标
- 右键属性 → 详细信息（产品名、版本、作者）
- 运行后任务栏 / Alt+Tab 图标
- 关于对话框

**换图标流程：**
1. 把你的图片覆盖 `assets/source_icon.png`（任意 PNG/JPG，建议 ≥256×256，正方形最佳；非正方形会自动居中裁剪）
2. ⚠️ **先关闭正在运行的桌宠**（否则 EXE 被占用）
3. 运行 `python build.py`
4. 产出 `dist/糊宠.exe`，图标、属性、任务栏全部更新

> 想改属性面板里的"产品名 / 作者"，编辑 `build.py` 顶部的 `APP_TITLE` / `COMPANY` 常量即可。

---

## 一次性准备（只需做一次）

### 1. 创建 GitHub 仓库
1. 登录 GitHub，点右上角 **+ → New repository**
2. 仓库名填 `DesktopPet_YMJH`，选 **Public**（公开），勾选 **Add a README**
3. 点 **Create repository**

### 2. 上传 version.json
1. 在新仓库页面点 **Add file → Upload files**
2. 把本目录的 `version.json` 拖进去，提交

### 3. 改程序里的 URL
打开 `desktop_pet.py`，确认这两行的用户名是你的（已替换为 `HannahZ-Mulan`）：
```python
VERSION_CHECK_URL = "https://raw.githubusercontent.com/HannahZ-Mulan/DesktopPet_YMJH/main/version.json"
VERSION_CHECK_URL_CDN = "https://cdn.jsdelivr.net/gh/HannahZ-Mulan/DesktopPet_YMJH@main/version.json"
```
（如需更换账号，改这里的用户名即可）

---

## 每次发布更新（推荐：脚本化流程，2 条命令）

> 自 v1.1.0 起，发版流程已脚本化。`build.py` 会自动校验版本号一致性（APP_VERSION / version.json / Release tag 三处必须一致，否则阻断打包），`publish.py` 会自动算 SHA256 + 生成 version.json 草稿。

### 第 1 步：改版本号 + changelog
打开 `desktop_pet.py`，把：
```python
APP_VERSION = "1.1.0"
```
改成新版本号，例如 `"1.2.0"`。

> 同时建议改 `version.json` 的 `changelog` 字段（发布说明），让网友看到更新内容。

### 第 2 步：打包（含版本号一致性校验）
⚠️ **先关闭正在运行的桌宠**（否则 `dist/糊宠.exe` 被占用，打包会报"拒绝访问"）。

在项目目录运行：
```bash
python build.py
```
脚本会：
1. **预检版本号一致性**：`APP_VERSION` / `version.json.version` / `download_url(s)` 的 tag 三处必须一致；不一致直接 `sys.exit(1)` 阻断打包（避免历史 bug 重现）。
2. 自动完成：① 用 `assets/source_icon.png` 生成多尺寸图标 → ② 生成版本资源 → ③ 调 PyInstaller 打包。
3. 产出 `dist/糊宠.exe`。

> **预检失败怎么修**：按报错提示三处对齐即可。通常是忘了同步 `version.json` 里的 `version` 和 `download_url(s)` 的 tag。

### 第 3 步：生成 version.json 草稿（自动算 SHA256）
```bash
python publish.py
```
脚本会：
1. 计算 `dist/糊宠.exe` 的真实 SHA256。
2. 从 `version.json` 推断 `owner/repo/exe`，生成 3 个默认镜像 URL（GitHub 权威源 + gh-proxy + ghfast.top）。
3. 写出 `version.json.draft`（**不覆盖 version.json**），并打印 4 步发布清单。

> **重要**：sha256 是针对**当前打包出的 EXE**算的。如果你重打包了 EXE（即使代码没变，PyInstaller 输出也可能略有差异），必须重跑 `publish.py` 让 sha256 跟实际发布的文件一致。

### 第 4 步：手动执行发布清单（publish.py 会打印）
按 `publish.py` 输出的提示：
1. **上传 EXE 到 GitHub Release**（tag 必须是 `vX.Y.Z` 格式，与 APP_VERSION 一致）。
2. **确认 draft 无误后覆盖 version.json**：
   ```bash
   copy /Y version.json.draft version.json   # Windows
   # 或 cp version.json.draft version.json    # 其他
   ```
3. **git 提交 + 打 tag**：
   ```bash
   git add version.json
   git commit -m "release vX.Y.Z"
   git push
   git tag vX.Y.Z
   git push origin vX.Y.Z
   ```
4. **在群内通知网友更新**。

### 完成！
用户下次启动桌宠时，会自动检测到新版本并提示更新。

---

## 备选：纯手动流程（不依赖 publish.py）

如果你不想用 `publish.py`，也可以手动维护 `version.json`：

### 第 1 步：改版本号
（同上）

### 第 2 步：打包
（同上，仍会做版本号一致性校验）

### 第 3 步：上传 EXE 到 GitHub Release
1. 在仓库页面点 **Releases → Create a new release**
2. **Choose a tag** 填 `v1.2.0`（和版本号一致）→ 点 **Create new tag**
3. **Release title** 填 `v1.2.0`
4. 把 `dist/糊宠.exe` 拖到 **Attach binaries** 区域
5. 点 **Publish release**

### 第 4 步：手动改 version.json
按下方"version.json Schema（v2）"完整字段手动编辑，关键点：
- `version` 必须 == APP_VERSION
- `download_url` 和 `download_urls[*]` 的 tag 段（`vX.Y.Z`）必须 == APP_VERSION
- `sha256` 是 EXE 的 SHA256（PowerShell 算：`Get-FileHash dist\糊宠.exe -Algorithm SHA256`），缺失时新客户端会跳过校验
- 回到 GitHub 仓库，编辑 `version.json` 并 Commit

---

## version.json Schema（v2，自 v1.1.0 起）

```json
{
  "version": "1.1.0",
  "update_date": "2026-08-11",
  "download_url": "https://github.com/HannahZ-Mulan/DesktopPet_YMJH/releases/download/v1.1.0/糊宠.exe",
  "download_urls": [
    "https://github.com/HannahZ-Mulan/DesktopPet_YMJH/releases/download/v1.1.0/糊宠.exe",
    "https://gh-proxy.com/https://github.com/HannahZ-Mulan/DesktopPet_YMJH/releases/download/v1.1.0/糊宠.exe",
    "https://ghfast.top/https://github.com/HannahZ-Mulan/DesktopPet_YMJH/releases/download/v1.1.0/糊宠.exe"
  ],
  "sha256": "f7e23fa275202d4e522d4a5616bfb40c2384c14ae9c6bae4a47a2c68061df44d",
  "changelog": "更新链路加固：多源下载 fallback、SHA256 完整性校验、失败可观测日志、SSL 白名单、发布脚本化。"
}
```

### 字段说明

| 字段 | 必填 | 说明 |
|---|---|---|
| `version` | ✅ | 远端最新版本号（与 APP_VERSION 同格式，如 `1.1.0`） |
| `update_date` | ✅ | 发版日期（展示用） |
| `changelog` | ✅ | 更新说明（展示用） |
| `download_url` | ✅(v1 兼容) | **保留**，向后兼容老客户端。值 = `download_urls[0]` |
| `download_urls` | 新增 | **新客户端优先读这个**。顺序即优先级，第 0 项是 GitHub 权威源，后续是加速镜像 |
| `sha256` | 新增 | EXE 的 SHA256（64 位 hex）。缺失时新客户端跳过校验（兼容旧 version.json） |

### 兼容性

- **老网友的旧 EXE**（只认 `download_url`）→ 读 v2 schema 仍能正常更新（忽略新字段）。
- **新网友的新 EXE** → 读 v1 schema（只有 `download_url`，没有 `download_urls` / `sha256`）也能更新（fallback 到单 URL + 跳过校验）。
- **加新镜像**：只需往 `download_urls` 数组里加一项，**不用发新版客户端**（数据驱动设计）。

---

## 用户端会发生什么
- 启动后 3 秒静默检查（不打扰）
- 有新版 → 右键菜单「检查更新」会弹出更新提示
- 点「立即更新」→ 自动尝试多个下载源（GitHub 失败自动 fallback 到镜像）→ 进度条 → SHA256 校验 → 自动替换 → 重启
- 失败时按错误码提示不同文案：
  - `network`：网络问题或被拦截，建议检查网络/手动下载
  - `sha256_mismatch`：文件校验未通过（可能被篡改），已自动中止安装
  - `canceled`：用户主动取消

## 排查日志（update.log）
更新过程中所有关键事件都记录在 `update.log`（与 EXE 同目录），格式：
```
[2026-08-11 14:30:01] [CHECK] OK url=... remote=1.1.0
[2026-08-11 14:30:15] [DOWNLOAD] FAIL url=... type=HTTPError msg=...
[2026-08-11 14:30:45] [DOWNLOAD] OK url=... size=89247123 sha256=ab12...
[2026-08-11 14:31:02] [VERIFY] OK
[2026-08-11 14:31:05] [INSTALL] START pid=12345
[2026-08-11 14:31:00] [SSL] WARN host=gh-proxy.com cert_check=relaxed
```
网友报"更新失败"时，让他把这个文件发给你即可定位问题。日志不含敏感信息（只有 URL / 版本号 / 异常类型）。

## 常见问题
- **用户下不动**：客户端已内置多源 fallback（GitHub → gh-proxy → ghfast.top），任一可用即可；都不行让用户用浏览器打开 `download_url` 手动下，或开梯子。
- **用户没收到更新提示**：确认 `version.json` 的 `version` 比 `APP_VERSION` 大，且已 commit + push。
- **想强制所有用户更新**：在 changelog 里写明，或把 `download_url(s)` 换成最新版。
- **想换/加镜像**：改 `version.json` 的 `download_urls` 数组即可，**不用改代码、不用发新版客户端**。镜像模板在 `publish.py` 的 `DEFAULT_MIRROR_TEMPLATES` 里。
- **build.py 报"版本号不一致"**：三处（APP_VERSION / version.json.version / download_url tag）必须对齐。详见上方第 2 步说明。
